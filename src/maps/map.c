/*
** EPITECH PROJECT, 2022
** B-MUL-100-LYN-1-1-myrunner-ilhan.neuville
** File description:
** map
*/

#include "../include/graph.h"

/* int lengh_map(char **av)
{
    struct stat sb;
    if (stat(av[1], &sb) == -1) {
        return (NULL);
    }
    int fd = open(av[1], O_RDONLY);
    char buff[sb.st_size + 1];
    buff[sb.st_size + 1] = '\0';
    int size = read(fd, buff, sb.st_size);
    printf("")
}

void map(initialisation *s, personnage_t *p, char **av)
{
    fopen(av[])
} */